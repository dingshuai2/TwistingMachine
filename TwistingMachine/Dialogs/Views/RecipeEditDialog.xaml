<MetroControls:MetroWindow x:Class="TwistingMachine.Dialogs.Views.RecipeEditDialog"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:MetroControls="http://metro.mahapps.com/winfx/xaml/controls"
                      xmlns:prism="http://prismlibrary.com/"
                      prism:ViewModelLocator.AutoWireViewModel="True"
                      mc:Ignorable="d"
                      Title="编辑配方"
                      Width="400" Height="400"
                      WindowStartupLocation="CenterOwner"
                      ShowCloseButton="True" ShowMinButton="False" ShowMaxRestoreButton="False">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Grid.Row="0" Background="#0066CC">
            <TextBlock Text="编辑配方" FontSize="20" FontWeight="Bold" Foreground="White" Margin="20,0,0,0" VerticalAlignment="Center"/>
        </Grid>

        <!-- 编辑内容 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <!-- 配方名称 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="配方名称" Width="100" FontSize="14"/>
                <TextBox Width="200" Height="25" Text="{Binding Recipe.RecipeName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14"/>
            </StackPanel>

            <!-- 线芯面积 -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="线芯面积" Width="100" FontSize="14"/>
                <TextBox Width="80" Height="25" Text="{Binding Recipe.CoreArea, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14"/>
                <TextBlock Text="mm²" Margin="5,0,0,0" FontSize="14"/>
            </StackPanel>

            <!-- 导线外径 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="导线外径" Width="100" FontSize="14"/>
                <TextBox Width="80" Height="25" Text="{Binding Recipe.WireOuterDiameter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14"/>
                <TextBlock Text="mm" Margin="5,0,0,0" FontSize="14"/>
            </StackPanel>

            <!-- 绞后长度 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="绞后长度" Width="100" FontSize="14"/>
                <TextBox Width="80" Height="25" Text="{Binding Recipe.TwistedLength, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14"/>
                <TextBlock Text="mm" Margin="5,0,0,0" FontSize="14"/>
            </StackPanel>

            <!-- 左开口 -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="左开口" Width="100" FontSize="14"/>
                <TextBox Width="80" Height="25" Text="{Binding Recipe.LeftEndOpening, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14"/>
                <TextBlock Text="mm" Margin="5,0,0,0" FontSize="14"/>
            </StackPanel>

            <!-- 右开口 -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="右开口" Width="100" FontSize="14"/>
                <TextBox Width="80" Height="25" Text="{Binding Recipe.RightEndOpening, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14"/>
                <TextBlock Text="mm" Margin="5,0,0,0" FontSize="14"/>
            </StackPanel>

            <!-- 过绞量 -->
            <StackPanel Grid.Row="6" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="过绞量" Width="100" FontSize="14"/>
                <TextBox Width="80" Height="25" Text="{Binding Recipe.OverTwistAmount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14"/>
                <TextBlock Text="%" Margin="5,0,0,0" FontSize="14"/>
            </StackPanel>
        </Grid>

        <!-- 按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="修改" Width="100" Height="35" Margin="10" Command="{Binding SaveCommand}"/>
            <Button Content="取消" Width="100" Height="35" Margin="10" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</MetroControls:MetroWindow>